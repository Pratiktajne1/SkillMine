using System;
using System.Collections.Generic;
using System.Text;

namespace SkillMine.ARRAY.Sum_of_Array
{
    class Sum_of_ArrayNo
    {
        int sum = 0;
       
        public int Display(int e)
        {
            if(e%2==0)
            {
                return sum = sum + e;
            }
            return sum = sum + 0;
        }
    }
}
